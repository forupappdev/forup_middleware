unit core.sql.engine;

interface
uses core.sql.builder, core.sql.entity, forup.util.types, System.Classes,
System.StrUtils, System.Math, System.SysUtils, System.DateUtils;

type
  TFUPORM<T : class, constructor> = class
   private
    FConn: IDBConnection;
   public
    constructor Create(AConn: IDBConnection);
  end;

implementation

end.
