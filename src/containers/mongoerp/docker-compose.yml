version: '3.8'

services:
  mongodb:
    image: mongoerp:latest
    container_name: mongodb_container
    restart: always
    env_file:
      - .env
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_INITDB_ROOT_USERNAME}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_INITDB_ROOT_PASSWORD}
    volumes:
      - ${MONGO_DATA_PATH}:/data/db
    ports:
      - "27017:27017"

