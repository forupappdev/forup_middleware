program mid_entity_sevice;

{$APPTYPE CONSOLE}

{$R *.res}

uses
  System.SysUtils,
  forup.log_unit in '..\packages\forup.log_unit.pas';

begin
  try
    { TODO -oUser -cConsole Main : Insert code here }
  except
    on E: Exception do
      Writeln(E.ClassName, ': ', E.Message);
  end;
end.
